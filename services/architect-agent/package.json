{
  "name": "@openbrain/architect-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "link": "tsx src/jobs/linker.ts",
    "tag": "tsx src/jobs/tagger.ts",
    "run": "npm run link && npm run tag",
    "smoke:run": "tsx src/jobs/smoke.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@openbrain/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.15.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
